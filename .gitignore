node_modules
dist
dist-electron
release
*.local
.DS_Store

# Downloaded by scripts/prepare-embedded-python.ps1 before packaging Windows installers.
bundle/python-runtime/

# Local / admin scripts — only DPS reader runtime is tracked
scripts/*
!scripts/odyssey-dps-reader.py
!scripts/requirements-dps.txt
!scripts/run-prepare-dps-python.mjs
!scripts/prepare-embedded-python.ps1

scripts/__pycache__/
*.pyc

# Local capture / debug dumps (do not commit).
combat_frames.jsonl
last.json
